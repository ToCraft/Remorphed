apply from: "https://raw.githubusercontent.com/ToCraft/GradleMaster/v2/common.gradle"

dependencies {
    modApi "dev.tocraft:craftedcore:${parent.name}-${rootProject.craftedcore_version}"
    modApi "dev.tocraft:walkers:${parent.name}-${rootProject.woodwalkers_version}"
}